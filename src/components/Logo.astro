---
// Logo component — uses the Nazaryah logo image at /public/images/nazaryah-logo.png
interface Props {
  variant?: 'full' | 'icon-only';
  className?: string;
}
const { variant = 'full', className = '' } = Astro.props;
---

{variant === 'full' ? (
  <div class={`logo-full ${className}`}>
    <img
      src="/images/nazaryah-wide.png"
      alt="Nazaryah — Separated Unto Yah"
      class="logo-img"
      loading="eager"
      decoding="async"
    />
  </div>
) : (
  <img
    src="/images/nazaryah-wide.png"
    alt="Nazaryah"
    class={`logo-img logo-icon-only ${className}`}
    loading="eager"
    decoding="async"
  />
)}

<style>
  .logo-full {
    display: inline-flex;
    align-items: center;
  }
  .logo-img {
    display: block;
    height: 110px;
    width: auto;
    max-width: 520px;
    object-fit: contain;
    transition: opacity 0.25s ease;
  }
  .logo-full:hover .logo-img,
  .logo-icon-only:hover {
    opacity: 0.9;
  }
  .logo-icon-only {
    height: 80px;
    width: auto;
  }
  @media (max-width: 900px) {
    .logo-img { height: 88px; max-width: 420px; }
  }
  @media (max-width: 640px) {
    .logo-img { height: 70px; max-width: 320px; }
  }
</style>
