---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// Get all published posts
const allPosts = await getCollection('posts', ({ data }) => {
  return data.draft !== true;
});

// Sort by date, most recent first
const sortedPosts = allPosts.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const allCategories = [
  'The Scrolls',
  'Scripture Unfiltered',
  'Buried in Plain Sight',
  'Parables',
  'The Trinity Files',
  'Word Studies',
  'The Law Still Stands',
  'The Feasts',
];

// Hover card content for each category — styled like manuscript field notes
const categoryMeta: Record<string, { subtitle: string; description: string; verse: string }> = {
  'The Scrolls': {
    subtitle: '— THE SCROLLS —',
    description: 'These are focused book studies — narrowed down to a single theme, subject, or argument. The full book, distilled to what actually matters. No filler, no fluff, just what the author found and whether Scripture backs it up.',
    verse: '"All Scripture is breathed out by God." — 2 Timothy 3:16'
  },
  'Scripture Unfiltered': {
    subtitle: '— SCRIPTURE UNFILTERED —',
    description: 'Passage by passage. Verse by verse. No denominational filter, no traditional spin — just the text opened up the way it was written to be read. Some of it will wreck what you were taught. That\'s the point.',
    verse: '"Study to show yourself approved." — 2 Timothy 2:15'
  },
  'Buried in Plain Sight': {
    subtitle: '— BURIED IN PLAIN SIGHT —',
    description: 'This is the historical record of how Christianity drifted — where its doctrines actually came from, what councils decided them, what Rome enforced, and what the Reformation refused to fix. Church history that the church doesn\'t teach.',
    verse: '"Ask for the ancient paths, where the good way is." — Jeremiah 6:16'
  },
  'Parables': {
    subtitle: '— PARABLES —',
    description: 'These are not studies of biblical parables — these are original parables written to carry hard truth in a way that a straight lecture never could. They\'re meant to be engaging. The truth inside them is anything but comfortable.',
    verse: '"To you it has been given to know the secrets of the kingdom." — Matthew 13:11'
  },
  'The Trinity Files': {
    subtitle: '— THE TRINITY FILES —',
    description: 'Every verse modern Christianity uses to defend the Trinity doctrine — examined one by one against the original text. Not opinion. Not tradition. Just Scripture, opened up and put on the stand.',
    verse: '"Hear, O Israel: Yahuah our God, Yahuah is one." — Deuteronomy 6:4'
  },
  'Word Studies': {
    subtitle: '— WORD STUDIES —',
    description: 'His name was changed. Key words were swapped. Covenantal meaning was buried under centuries of translation and tradition. These studies go back to the Hebrew and Greek to recover what the text actually says — before the translators got to it.',
    verse: '"My people are destroyed for lack of knowledge." — Hosea 4:6'
  },
  'The Law Still Stands': {
    subtitle: '— THE LAW STILL STANDS —',
    description: 'Scripture never said the Torah was abolished. That idea came from centuries of misread letters and mistranslated verses. These studies go back to the text and make the case that the Law was never nailed to anything — and that keeping it still matters.',
    verse: '"Your Torah is truth." — Psalm 119:142'
  },
  'The Feasts': {
    subtitle: '— THE FEASTS —',
    description: 'Yahuah set appointed times in Scripture — specific, named, and tied to His covenant with His people. Christianity replaced them with holidays borrowed from somewhere else entirely. Both calendars are laid out here. You decide which one belongs to Him.',
    verse: '"These are my appointed feasts." — Leviticus 23:2'
  },
};
---

<BaseLayout
  title="Biblical Studies & Theological Insights - Nazaryah Blog"
  description="Explore in-depth biblical studies covering exegesis, theology, historical context, and original language word studies."
>
  <div class="py-16 md:py-20">
    <div class="container mx-auto px-6">
      <div class="max-w-7xl mx-auto">

        <!-- Header -->
        <div class="text-center mb-12">
          <h1 class="text-4xl md:text-5xl font-bold mb-4" style="font-family: var(--font-display); color: var(--deep-earth);">
            Biblical Studies
          </h1>
          <p class="text-lg max-w-3xl mx-auto" style="color: var(--ink-faded);">
            In-depth explorations of Scripture through original languages, historical context, and scholarly analysis
          </p>
        </div>

        <!-- Category Filter -->
        <div class="mb-16">
          <div class="flex flex-wrap gap-3 justify-center">

            <!-- All Posts — no tooltip -->
            <button class="dig-filter active" data-category="all">
              <span class="dig-filter-inner">All Posts</span>
            </button>

            <!-- Category buttons with hover tooltips -->
            {allCategories.map((category) => (
              <div class="dig-filter-wrapper">
                <button class="dig-filter" data-category={category}>
                  <span class="dig-filter-inner">{category}</span>
                </button>

                {categoryMeta[category] && (
                  <div class="dig-tooltip" role="tooltip">
                    <!-- Dark header bar -->
                    <div class="dig-tooltip-header">
                      <span>△</span>
                      <span>{category}</span>
                      <span>△</span>
                    </div>
                    <!-- Body -->
                    <div class="dig-tooltip-body">
                      <p class="dig-tooltip-subtitle">{categoryMeta[category].subtitle}</p>
                      <p class="dig-tooltip-description">{categoryMeta[category].description}</p>
                      <div class="dig-tooltip-divider">
                        <span class="dig-tooltip-diamond">◆</span>
                      </div>
                      <p class="dig-tooltip-verse">{categoryMeta[category].verse}</p>
                    </div>
                  </div>
                )}
              </div>
            ))}
          </div>
        </div>

        <!-- Posts Grid -->
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-5" id="posts-grid">
          {sortedPosts.map((post) => (
            <article
              class="post-item card group flex flex-col overflow-hidden"
              data-category={post.data.category}
            >
              <div class="p-6 flex flex-col flex-1">
                <div class="tag mb-3" style="color: var(--bronze);">
                  {post.data.category}
                </div>
                <h2 class="post-title text-lg font-semibold mb-3 leading-snug" style="font-family: var(--font-display); color: var(--deep-earth);">
                  <a
                    href={`/blog/${post.slug}`}
                    class="hover:text-[var(--bronze)] transition-colors"
                    style="color: var(--deep-earth);"
                  >
                    {post.data.title}
                  </a>
                </h2>
                <p class="text-sm leading-relaxed mb-5 line-clamp-4 flex-1" style="color: var(--ink-faded);">
                  {post.data.description}
                </p>

                <div class="flex items-center justify-between text-xs pt-4 border-t border-[var(--sand-dark)]" style="color: var(--ink-faded); font-family: var(--font-body);">
                  <time datetime={post.data.date.toISOString()}>
                    {post.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}
                  </time>
                  <a
                    href={`/blog/${post.slug}`}
                    class="font-medium group-hover:translate-x-1 transition-transform inline-flex items-center gap-1"
                    style="color: var(--bronze);"
                  >
                    Read <span aria-hidden="true">&rarr;</span>
                  </a>
                </div>
              </div>
            </article>
          ))}
        </div>

        {sortedPosts.length === 0 && (
          <div class="text-center py-20">
            <p class="text-lg" style="color: var(--ink-faded);">
              No posts published yet. Check back soon!
            </p>
          </div>
        )}
      </div>
    </div>
  </div>
</BaseLayout>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const filterButtons = document.querySelectorAll('.dig-filter');
    const postCards = document.querySelectorAll('.post-item');

    filterButtons.forEach(button => {
      button.addEventListener('click', () => {
        const category = button.getAttribute('data-category');

        // Update active state
        filterButtons.forEach(btn => btn.classList.remove('active'));
        button.classList.add('active');

        // Filter posts
        postCards.forEach(card => {
          const cardCategory = card.getAttribute('data-category');
          if (category === 'all' || cardCategory === category) {
            (card as HTMLElement).style.display = 'block';
          } else {
            (card as HTMLElement).style.display = 'none';
          }
        });
      });
    });
  });
</script>

<style>
  /* ── DIG SITE FILTER BUTTONS ── */

  /* Wrapper for tooltip positioning */
  .dig-filter-wrapper {
    position: relative;
  }

  /* The button itself */
  .dig-filter {
    position: relative;
    font-family: var(--font-display);
    font-size: 10px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--ink-faded);
    background: transparent;
    border: 1px solid var(--sand-dark);
    border-radius: 2px;
    padding: 0;
    cursor: pointer;
    transition: all 0.25s ease;
    /* Rough parchment fragment feel — slightly uneven corners */
    clip-path: polygon(
      0% 2px, 2px 0%,
      calc(100% - 2px) 0%, 100% 2px,
      100% calc(100% - 2px), calc(100% - 2px) 100%,
      2px 100%, 0% calc(100% - 2px)
    );
  }

  .dig-filter-inner {
    display: block;
    padding: 0.45rem 1rem;
  }

  /* Hover state — clay gradient, matches nav-link exactly */
  .dig-filter:hover {
    background: linear-gradient(135deg, var(--clay-hover), var(--clay), var(--clay-deep));
    color: #ffffff;
    border-color: var(--clay);
    box-shadow: 0 2px 8px rgba(150, 90, 62, 0.35);
  }

  /* Active/selected state — clay gradient stays */
  .dig-filter.active {
    background: linear-gradient(135deg, var(--clay-hover), var(--clay), var(--clay-deep));
    color: #ffffff;
    border-color: var(--clay);
    box-shadow: 0 2px 8px rgba(150, 90, 62, 0.35);
  }

  /* ── TOOLTIP CARD ── */

  .dig-tooltip {
    display: none;
    position: absolute;
    top: calc(100% + 10px);
    left: 50%;
    transform: translateX(-50%);
    width: 280px;
    z-index: 100;
    /* Parchment card border */
    border: 1px solid var(--color-border, #c8a878);
    border-radius: 3px;
    overflow: hidden;
    box-shadow:
      inset 0 0 30px rgba(75, 54, 33, 0.12),
      0 8px 32px rgba(0, 0, 0, 0.35),
      0 2px 8px rgba(0, 0, 0, 0.2);
    /* Prevent tooltip from being cut off */
    pointer-events: none;
  }

  /* Show on wrapper hover */
  .dig-filter-wrapper:hover .dig-tooltip {
    display: block;
  }

  /* Dark header bar — matches the image */
  .dig-tooltip-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background-color: #2b1d0e;
    color: var(--sand-dark, #c4a97d);
    font-family: var(--font-display);
    font-size: 9px;
    letter-spacing: 3px;
    text-transform: uppercase;
    padding: 0.55rem 0.9rem;
    gap: 0.5rem;
  }

  /* Divider lines on either side of the header text */
  .dig-tooltip-header span:first-child,
  .dig-tooltip-header span:last-child {
    opacity: 0.6;
    font-size: 8px;
  }

  /* Parchment body */
  .dig-tooltip-body {
    background-color: var(--color-surface, #f5e8c8);
    background-image: url("https://www.transparenttextures.com/patterns/p6.png");
    padding: 1rem 1.1rem 0.85rem;
  }

  /* Subtitle — "— UNEARTHING THE ANCIENT TEXT —" style */
  .dig-tooltip-subtitle {
    font-family: var(--font-display);
    font-size: 8px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--bronze, #8b6914);
    margin: 0 0 0.6rem;
    text-align: center;
  }

  /* Description text */
  .dig-tooltip-description {
    font-family: var(--font-body);
    font-size: 0.88rem;
    line-height: 1.6;
    color: var(--color-primary-text, #2b1d11);
    margin: 0 0 0.75rem;
  }

  /* Divider with diamond — matches image */
  .dig-tooltip-divider {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin: 0.6rem 0;
  }
  .dig-tooltip-divider::before,
  .dig-tooltip-divider::after {
    content: '';
    flex: 1;
    height: 1px;
    background-color: var(--color-border, #c8a878);
  }
  .dig-tooltip-diamond {
    font-size: 10px;
    color: var(--bronze, #8b6914);
    line-height: 1;
  }

  /* Italic Bible verse at bottom */
  .dig-tooltip-verse {
    font-family: var(--font-body);
    font-style: italic;
    font-size: 0.8rem;
    color: var(--ink-faded, #5c4033);
    text-align: center;
    margin: 0;
    line-height: 1.5;
  }

  /* ── Post card line clamp ── */
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .line-clamp-4 {
    display: -webkit-box;
    -webkit-line-clamp: 4;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  /* Prevent long words from being cut off in narrow 5-column grid cards */
  .post-title,
  .post-title a {
    overflow-wrap: break-word;
    word-wrap: break-word;
    hyphens: auto;
  }

  /* ── Dark mode adjustments ── */
  :global(.dark) .dig-filter {
    color: var(--stone-light);
    border-color: rgba(139, 125, 107, 0.4);
  }
  :global(.dark) .dig-filter:hover,
  :global(.dark) .dig-filter.active {
    background: linear-gradient(135deg, var(--clay-hover), var(--clay), var(--clay-deep));
    color: #ffffff;
    border-color: var(--clay);
  }

  :global(.dark) .dig-tooltip-header {
    background-color: #1a1208;
    color: var(--gold, #c5a44e);
  }

  :global(.dark) .dig-tooltip-body {
    background-color: var(--color-dark-surface, #2a1d10);
    background-image: url("https://www.transparenttextures.com/patterns/dark-wall.png");
  }

  :global(.dark) .dig-tooltip-subtitle {
    color: var(--gold, #c5a44e);
  }

  :global(.dark) .dig-tooltip-description {
    color: var(--color-dark-text, #f0e0bf);
  }

  :global(.dark) .dig-tooltip-verse {
    color: var(--color-dark-secondary-text, #c4ad84);
  }

  :global(.dark) .dig-tooltip-divider::before,
  :global(.dark) .dig-tooltip-divider::after {
    background-color: rgba(139, 125, 107, 0.4);
  }

  :global(.dark) .dig-tooltip-diamond {
    color: var(--gold, #c5a44e);
  }
</style>
