---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { movements, roadNote, terms, hasWord, hasBreak, slugify } from '../../../data/legal-lexicon.js';

const byMovement = movements.map((m) => ({ m, rows: terms.filter((t) => t.movement === m.id) }));
const azTerms = [...terms].sort((a, b) => a.term.localeCompare(b.term));
const romOf = (mid) => movements[mid - 1].rom;
const termHref = (t) => `/torah/legal-lexicon/${slugify(t.term)}`;
---

<BaseLayout
  title="The Legal Lexicon of Scripture — Torah | Nazaryah"
  description="The whole Bible is a courtroom. These are its words — seven movements from the bench to the final verdict."
>
  <div class="ll">
    <div class="wrap">

      <p class="crumb"><a href="/torah">Torah</a> &rsaquo; Legal Lexicon</p>

      <section class="hero">
        <span class="eyebrow">The Legal Lexicon of Scripture</span>
        <h1>The whole Bible is a courtroom.</h1>
        <p class="thesis">These are its words — and once you see them, you cannot unsee it.</p>
        <p>Scripture is not a devotional book with a few legal words in it. It is a legal book, and the warmth readers feel is the warmth of a covenant kept. Every term below — even the soft, familiar ones — is a courtroom word doing courtroom work.</p>
        <p>The words are arranged as a road: seven movements that walk from the bench itself to the final verdict. Read them in order to walk the whole trial, or look up any single word from A to Z.</p>
      </section>

      <section class="road" id="road">
        <div class="road-head">
          <span class="eyebrow">The Road</span>
          <h2>The Seven Movements</h2>
          <p class="sub">every word in the lexicon lives in one of these seven</p>
        </div>
        <div class="steps">
          {byMovement.map(({ m, rows }) => (
            <a class="rstep" href={`#m${m.id}`} onclick="ensureMove()">
              <span class="rn">{m.rom}</span>
              <span class="rbody">
                <span class="rname">{m.name}</span>
                <span class="rdesc">{m.desc}</span>
                <span class="rct">{rows.length} word{rows.length === 1 ? '' : 's'}</span>
              </span>
            </a>
          ))}
        </div>
        <p class="roadnote" set:html={roadNote.replace(/\((IV|V|VII)\)/g, '<b>($1)</b>')} />
      </section>

      <div class="controls">
        <div class="toggle">
          <button id="btnMove" class="on" onclick="setView('move')">By Movement</button>
          <button id="btnAz" onclick="setView('az')">A&ndash;Z</button>
        </div>
        <div class="search"><input id="q" type="text" placeholder="Filter words&hellip;" oninput="applyFilter()"></div>
        <span class="count" id="count">{terms.length} terms · growing</span>
      </div>

      <div id="viewMove">
        {byMovement.map(({ m, rows }) => (
          <section class="msec" id={`m${m.id}`}>
            <h3><span class="rom">{m.rom}</span> {m.name}</h3>
            <p class="msub">{m.desc}</p>
            <ul class="termrow">
              {rows.map((t) => (
                <li data-name={t.term.toLowerCase()}>
                  <span class="termnum">{t.n}</span>
                  {hasBreak(t) && <span class="brk-dot" title="At work in the counterfeit court" aria-label="break term">&#9679;</span>}
                  {hasWord(t.n)
                    ? <a class="termlink" href={termHref(t)}>{t.term}</a>
                    : <span class="termplain">{t.term}</span>}
                  <span class="termgloss">{t.gloss}</span>
                </li>
              ))}
            </ul>
          </section>
        ))}
      </div>

      <div id="viewAz" class="hidden">
        <section class="msec">
          <ul class="termrow">
            {azTerms.map((t) => (
              <li data-name={t.term.toLowerCase()}>
                <span class="termnum">{t.n}</span>
                {hasBreak(t) && <span class="brk-dot" title="At work in the counterfeit court" aria-label="break term">&#9679;</span>}
                {hasWord(t.n)
                  ? <a class="termlink" href={termHref(t)}>{t.term}</a>
                  : <span class="termplain">{t.term}</span>}
                <span class="termgloss">{t.gloss}</span>
                <a class="mvchip" href={`#m${t.movement}`} onclick="ensureMove()">{romOf(t.movement)}</a>
              </li>
            ))}
          </ul>
        </section>
      </div>

      <p class="foot">nazaryah.com &nbsp;·&nbsp; The Legal Lexicon of Scripture</p>

    </div>
  </div>
</BaseLayout>

<script is:inline define:vars={{ TOTAL: terms.length }}>
  const el = (id) => document.getElementById(id);
  let view = 'move';
  window.setView = function (v) {
    view = v;
    el('viewMove').classList.toggle('hidden', v !== 'move');
    el('viewAz').classList.toggle('hidden', v !== 'az');
    el('btnMove').classList.toggle('on', v === 'move');
    el('btnAz').classList.toggle('on', v === 'az');
    applyFilter();
  };
  window.ensureMove = function () { if (view !== 'move') setView('move'); };
  window.applyFilter = function () {
    const q = el('q').value.trim().toLowerCase();
    const root = view === 'move' ? el('viewMove') : el('viewAz');
    let shown = 0;
    root.querySelectorAll('li').forEach((li) => {
      const hit = !q || li.dataset.name.includes(q);
      li.classList.toggle('hidden', !hit); if (hit) shown++;
    });
    if (view === 'move') {
      root.querySelectorAll('.msec').forEach((s) => {
        const any = [...s.querySelectorAll('li')].some((li) => !li.classList.contains('hidden'));
        s.classList.toggle('hidden', !any);
      });
    }
    el('count').textContent = q ? `${shown} of ${TOTAL} shown` : `${TOTAL} terms · growing`;
  };
</script>

<style>
  /* Torah section — matches the Trinity page theme (fixed dark, cream serif, gold accents). */
  .ll{
    background:linear-gradient(180deg,#0e0904 0%,#1a1208 20%,#160f07 100%);
    color:#d9c9a8;
    font-family:'Crimson Pro','Crimson Text',Georgia,serif;line-height:1.7;font-size:18px;
  }
  .ll .wrap{max-width:900px;margin:0 auto;padding:34px 26px 90px;}
  .ll .eyebrow{font-family:'Cinzel',serif;font-size:11px;letter-spacing:.24em;text-transform:uppercase;color:#8a6028;font-weight:600;}
  .ll a{color:inherit;}

  .ll .crumb{font-family:'Cinzel',serif;font-size:10px;letter-spacing:.2em;text-transform:uppercase;color:#8a6028;
    border:1px solid #3a2510;border-radius:2px;padding:4px 9px;display:inline-block;background:rgba(20,13,5,.5);}
  .ll .crumb a{color:#c4a46b;text-decoration:none;}

  .ll .hero{padding:40px 0 8px;}
  .ll .hero h1{font-family:'Cinzel',serif;font-size:clamp(2rem,5vw,46px);line-height:1.08;color:#f0e0bf;margin:14px 0 10px;letter-spacing:-.01em;}
  .ll .thesis{font-size:22px;color:#c4a46b;font-style:italic;margin:0 0 18px;}
  .ll .hero p{font-size:18px;color:#d9c9a8;max-width:62ch;margin:0 0 12px;line-height:1.8;}

  .ll .road{margin:36px 0 10px;background:rgba(20,13,5,.5);border:1px solid #3a2510;border-radius:4px;padding:30px 34px 34px;}
  .ll .road-head h2{font-family:'Cinzel',serif;font-size:22px;color:#f0e0bf;margin:6px 0 2px;}
  .ll .road-head .sub{font-size:14px;color:#9a8763;font-style:italic;margin:0 0 4px;}
  .ll .steps{margin-top:22px;position:relative;}
  .ll .steps::before{content:"";position:absolute;left:26px;top:8px;bottom:8px;width:2px;background:#3a2510;}
  .ll .rstep{display:flex;gap:20px;padding:12px 0;align-items:flex-start;text-decoration:none;position:relative;}
  .ll .rstep .rn{flex:0 0 54px;height:54px;border-radius:50%;background:#160f07;border:2px solid #8a6028;
    color:#c4a46b;display:flex;align-items:center;justify-content:center;font-family:'Cinzel',serif;font-size:17px;font-weight:700;letter-spacing:.02em;z-index:1;transition:all .15s;}
  .ll .rstep:hover .rn{background:#8a6028;color:#160f07;border-color:#c4a46b;}
  .ll .rstep .rbody{padding-top:5px;}
  .ll .rstep .rname{font-family:'Cinzel',serif;color:#f0e0bf;font-weight:600;font-size:18px;display:block;}
  .ll .rstep:hover .rname{color:#c4a46b;}
  .ll .rstep .rdesc{color:#b09a6f;font-size:15.5px;margin-top:3px;line-height:1.6;}
  .ll .rstep .rct{color:#8a6f45;font-size:12px;letter-spacing:.06em;margin-top:4px;text-transform:uppercase;font-family:'Cinzel',serif;}
  .ll .roadnote{font-size:13.5px;color:#9a8763;font-style:italic;margin-top:20px;border-top:1px solid #2e1f0d;padding-top:16px;}
  .ll .roadnote b{color:#e8d5a8;font-style:normal;}

  .ll .controls{display:flex;gap:16px;align-items:center;flex-wrap:wrap;margin:40px 0 6px;
    padding:14px 0;border-bottom:1px solid #3a2510;}
  .ll .toggle{display:inline-flex;border:1px solid #8a6028;border-radius:3px;overflow:hidden;}
  .ll .toggle button{font-family:'Cinzel',serif;font-size:13px;letter-spacing:.06em;padding:8px 18px;border:0;cursor:pointer;background:transparent;color:#c4a46b;text-transform:uppercase;}
  .ll .toggle button.on{background:#8a6028;color:#160f07;}
  .ll .search{flex:1;min-width:180px;}
  .ll .search input{width:100%;font-family:inherit;font-size:15px;padding:9px 13px;border:1px solid #3a2510;border-radius:3px;background:#160f07;color:#e0d2b2;}
  .ll .search input::placeholder{color:#8a6f45;font-style:italic;}
  .ll .count{font-size:12px;color:#8a6f45;letter-spacing:.06em;font-family:'Cinzel',serif;text-transform:uppercase;}

  .ll .msec{margin-top:34px;scroll-margin-top:80px;}
  .ll .msec h3{font-family:'Cinzel',serif;font-size:14px;letter-spacing:.14em;text-transform:uppercase;color:#f0e0bf;margin:0 0 3px;display:flex;align-items:baseline;gap:10px;padding-bottom:.6rem;border-bottom:1px solid #3a2510;}
  .ll .msec h3 .rom{color:#8a6028;}
  .ll .msec .msub{font-size:14px;color:#c4a46b;font-style:italic;margin:12px 0 14px;}

  .ll .termrow{list-style:none;margin:0;padding:0;}
  .ll .termrow li{display:flex;align-items:baseline;gap:14px;padding:10px 0;border-top:1px solid #2e1f0d;}
  .ll .termrow li:first-child{border-top:0;}
  .ll .termnum{color:#8a6f45;font-size:13px;min-width:30px;text-align:right;}
  .ll .brk-dot{color:#e0655b;font-size:11px;line-height:1;margin:0 -6px 0 -4px;}
  .ll .termlink{color:#c4a46b;text-decoration:none;font-weight:600;border-bottom:1px dotted #6a4820;}
  .ll .termlink:hover{color:#f0e0bf;}
  .ll .termplain{color:#b09a6f;font-weight:600;opacity:.85;}
  .ll .termgloss{color:#9a8763;font-size:14.5px;font-style:italic;}
  .ll .mvchip{font-family:'Cinzel',serif;font-size:10px;letter-spacing:.06em;color:#8a6028;border:1px solid #3a2510;border-radius:2px;padding:1px 7px;text-decoration:none;margin-left:auto;white-space:nowrap;}
  .ll .mvchip:hover{border-color:#8a6028;color:#c4a46b;}

  .ll .hidden{display:none !important;}
  .ll .foot{text-align:center;color:#6a5a3a;font-family:'Cinzel',serif;font-size:11px;letter-spacing:.1em;margin-top:50px;}

  @media (max-width:620px){
    .ll{font-size:17px;}
    .ll .wrap{padding:24px 16px 60px;}
    .ll .hero h1{font-size:34px;} .ll .thesis{font-size:18px;}
    .ll .road{padding:22px 18px;}
    .ll .rstep .rn{flex-basis:44px;height:44px;font-size:15px;}
    .ll .steps::before{left:21px;}
    .ll .mvchip{display:none;}
  }
</style>
