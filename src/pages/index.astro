---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Nazaryah - Biblical Studies with Scholarly Integrity"
  description="Explore Scripture through original languages, fresh perspectives, and rich historical context. A modern biblical studies platform for serious learners."
>
  <!-- Hero Section -->
  <section class="hero-section relative overflow-hidden">
    <!-- Subtle grid texture overlay -->
    <div class="absolute inset-0 pointer-events-none" aria-hidden="true" style="
      background-image:
        repeating-linear-gradient(0deg, transparent, transparent 39px, rgba(0,0,0,0.022) 40px),
        repeating-linear-gradient(90deg, transparent, transparent 39px, rgba(0,0,0,0.022) 40px);
      z-index: 0;
    "></div>

    <div class="hero-grid relative z-10">
      <!-- LEFT: Logo panel (dark background matches logo image) -->
      <div class="logo-panel">
        <img
          src="/images/nazaryah-logo.png"
          alt="Nazaryah — Separated Unto Yah"
          class="logo-panel-img"
          loading="eager"
          decoding="async"
        />
      </div>

      <!-- RIGHT: Content -->
      <div class="content-panel">
        <h1 class="hero-headline">
          The Doctrine Was Built<br>
          To Keep You <em>Comfortable.</em>
        </h1>
        <div class="hero-rule"></div>
        <p class="hero-body">
          Nazaryah goes back to the source — original languages, restored names, and the Torah your church quietly skipped. No denominations. No comfortable answers. Just the text.
        </p>
        <div class="hero-cta-row">
          <a href="/blog" class="btn btn-primary">Explore Studies</a>
          <a href="/doctrines" class="start-here-btn" aria-label="Start here — Doctrines">
            <span class="start-here-badge"><span class="star">★</span> Start Here</span>
            <span class="start-here-label">Doctrines</span>
            <span class="start-here-arrow" aria-hidden="true">→</span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <style>
    .hero-section {
      width: 100%;
      padding: 1rem 1.5rem;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .hero-grid {
      display: grid;
      grid-template-columns: 5fr 8fr;
      width: 100%;
      max-width: 80rem;
      aspect-ratio: 5 / 1;
      overflow: hidden;
      border-radius: 4px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.22), 0 2px 8px rgba(0,0,0,0.15);
    }

    /* ── LEFT PANEL: dark, logo centered ── */
    .logo-panel {
      position: relative;
      background: #0f0a04;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem 1.25rem;
      overflow: hidden;
    }

    /* Subtle warm radial glow behind the logo */
    .logo-panel::before {
      content: '';
      position: absolute;
      inset: 0;
      background: radial-gradient(ellipse at 50% 45%, rgba(180, 120, 30, 0.18) 0%, transparent 70%);
      pointer-events: none;
    }

    /* Corner ornaments */
    .logo-panel::after {
      content: '';
      position: absolute;
      top: 10px; left: 10px;
      width: 20px; height: 20px;
      border-top: 1.5px solid rgba(196, 154, 78, 0.45);
      border-left: 1.5px solid rgba(196, 154, 78, 0.45);
    }

    .logo-panel-img {
      position: relative;
      z-index: 1;
      width: 100%;
      height: 85%;
      object-fit: contain;
    }

    /* ── RIGHT PANEL ── */
    .content-panel {
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 0.55rem;
      padding: 1rem 2rem 1rem 2rem;
    }

    .hero-headline {
      font-family: var(--font-display);
      font-size: clamp(1.2rem, 2vw, 1.75rem);
      font-weight: 700;
      line-height: 1.15;
      color: var(--color-primary-text);
      margin: 0;
    }

    .hero-headline em {
      font-style: italic;
      color: var(--gold-light, #c49a4e);
    }

    .hero-rule {
      width: 48px;
      height: 1.5px;
      background: var(--color-border-strong);
      opacity: 0.65;
    }

    .hero-body {
      font-family: var(--font-body);
      font-size: 0.95rem;
      line-height: 1.6;
      color: var(--color-secondary-text);
      margin: 0;
    }

    .hero-cta-row {
      display: flex;
      gap: 18px;
      flex-wrap: wrap;
      align-items: center;
      margin-top: 8px;
    }

    /* ── "Start Here · Doctrines" button ── */
    .start-here-btn {
      position: relative;
      display: inline-flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 6px;
      padding: 22px 28px 18px;
      min-width: 180px;
      background: linear-gradient(145deg, #e4bd6a 0%, #c49a4e 55%, #a57b2f 100%);
      color: #1a1206;
      text-decoration: none;
      font-family: var(--font-display, 'Playfair Display', Georgia, serif);
      border-radius: 14px 14px 28px 28px / 14px 14px 40px 40px;
      box-shadow:
        0 6px 18px rgba(0,0,0,0.22),
        inset 0 1px 0 rgba(255,240,200,0.55),
        inset 0 -3px 8px rgba(80,50,10,0.22);
      margin-top: 18px;
      transition: transform 0.25s ease, box-shadow 0.25s ease;
    }
    .start-here-btn:hover {
      transform: translateY(-2px);
      box-shadow:
        0 10px 24px rgba(0,0,0,0.28),
        inset 0 1px 0 rgba(255,240,200,0.6),
        inset 0 -3px 8px rgba(80,50,10,0.22);
    }

    .start-here-badge {
      position: absolute;
      top: -14px;
      left: 18px;
      background: #0f0a04;
      color: #e4bd6a;
      font-family: var(--font-display, 'Cinzel', 'Playfair Display', Georgia, serif);
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      padding: 6px 14px 5px;
      border-radius: 999px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.35);
      white-space: nowrap;
    }
    .start-here-badge .star {
      color: #e4bd6a;
      margin-right: 4px;
    }

    .start-here-label {
      font-size: 1.35rem;
      font-weight: 700;
      line-height: 1.1;
      color: #1a1206;
      margin-top: 6px;
    }
    .start-here-arrow {
      font-size: 1rem;
      color: #1a1206;
      line-height: 1;
      transition: transform 0.25s ease;
    }
    .start-here-btn:hover .start-here-arrow {
      transform: translateX(4px);
    }

    /* ── Responsive ── */
    @media (max-width: 640px) {
      .pillar-grid {
        grid-template-columns: 1fr !important;
      }
    }

    @media (max-width: 720px) {
      .hero-section {
        padding: 1.5rem 1rem;
      }

      .hero-grid {
        grid-template-columns: 1fr;
        aspect-ratio: unset;
        max-width: 100%;
      }

      .logo-panel {
        padding: 1.75rem 1.5rem;
        min-height: 180px;
      }

      .logo-panel-img {
        max-width: 220px;
      }

      .content-panel {
        padding: 1.75rem 1.5rem;
        gap: 0.75rem;
      }

      .hero-headline {
        font-size: 1.6rem;
      }
    }
  </style>

  <!-- ========== Homepage Sections (Four-Band Layout) ========== -->
  <div class="home-sections">
    <div class="home-container">

      <!-- ========= SECTION 1: FEATURED / EDITORIAL ========= -->
      <section class="home-section">
        <div class="section-header">
          <div class="section-eyebrow">The Latest</div>
          <div class="section-ornament"><span class="line"></span><span class="dot"></span><span class="line"></span></div>
          <h2 class="section-title">From the Desk</h2>
          <p class="section-sub">What's new this week — briefs, studies, and long-form from the library.</p>
        </div>

        <div class="home-grid">
          <!-- Watchman's Desk -->
          <a class="home-card card-watchman" href="/watchmans-desk">
            <div>
              <div class="watchman-mast">★ THE WATCHMAN'S DESK ★</div>
              <div class="date">22 APRIL 2026 · THREE BRIEFS</div>
              <h3 class="card-title">The Triumphal Arch Comes to Washington</h3>
            </div>
            <div class="article-body">
              On April 16 the federal Commission of Fine Arts approved a 250-foot Roman arch — copy of Napoleon's Sol Invictus monument. Unveiling targeted for July 4. The form is older than Rome. The date is older than the calendar.
            </div>
            <div class="card-cta">ALL FIELD NOTES →</div>
          </a>

          <!-- Studies -->
          <a class="home-card card-studies" href="/blog">
            <div>
              <div class="aleph">א</div>
              <div class="kicker" style="color:#5a3a1a;">SCRIPTURE UNFILTERED</div>
              <h3 class="card-title">The Studies</h3>
              <p class="card-body">Verse by verse in the original Hebrew and Greek — recovering what was buried under tradition, denomination, and translation drift.</p>
            </div>
            <div>
              <div class="latest-badge">
                <div class="label">★ LATEST STUDY ★</div>
                <div class="title">Grace, New Creation, and Covenant Loyalty</div>
              </div>
              <div class="card-cta">ALL BIBLE STUDIES →</div>
            </div>
          </a>

          <!-- Library -->
          <a class="home-card card-library" href="/books">
            <div>
              <div class="diamonds">◆ ◆ ◆</div>
              <div class="kicker">FROM THE LIBRARY</div>
              <h3 class="card-title">The Courtroom of Heaven</h3>
              <div class="sub-italic">Revelation Putting Modern Teaching on Trial</div>
              <p class="card-body">Long-form teaching — the chapter studies, the Court Appeals, and the exhibits — that won't fit inside a blog post.</p>
            </div>
            <div class="card-cta">BROWSE THE LIBRARY →</div>
          </a>
        </div>
      </section>

      <div class="divider-line"></div>

      <!-- ========= SECTION 2: THE COUNTERFEITS ========= -->
      <section class="home-section">
        <div class="section-header">
          <div class="section-eyebrow">What Modern Christianity Got Wrong</div>
          <div class="section-ornament"><span class="line"></span><span class="dot"></span><span class="line"></span></div>
          <h2 class="section-title">The Counterfeits</h2>
          <p class="section-sub">Six doctrines, symbols, and institutions the pulpit never held up to the text.</p>
        </div>

        <div class="home-grid">
          <!-- Christian or Demonic -->
          <a class="home-card card-demonic" href="/christian-or-demonic">
            <div>
              <div class="kicker" style="color:#ff6a3d;">A FIVE-PART EXAMINATION</div>
              <h3 class="card-title">Christian <em>or</em><br>Demonic?</h3>
              <p class="body-dark">Five pillars of modern Christianity. Five uncomfortable questions. <em>Five answers the pulpit never gave you.</em></p>
            </div>
            <div>
              <div class="pillars">Prayer &nbsp;|&nbsp; Music &nbsp;|&nbsp; Canon &nbsp;|&nbsp; Translations &nbsp;|&nbsp; Symbols</div>
              <div class="card-cta" style="padding-top:22px;">TAKE THE EXAMINATION →</div>
            </div>
          </a>

          <!-- Sun Worship -->
          <a class="home-card card-sun" href="/sun-worship">
            <div>
              <svg class="sun-icon" viewBox="0 0 40 40">
                <circle cx="20" cy="20" r="8" fill="#ffdca0"/>
                <g stroke="#ffdca0" stroke-width="1.5">
                  <line x1="20" y1="2" x2="20" y2="8"/>
                  <line x1="20" y1="32" x2="20" y2="38"/>
                  <line x1="2" y1="20" x2="8" y2="20"/>
                  <line x1="32" y1="20" x2="38" y2="20"/>
                  <line x1="7" y1="7" x2="11" y2="11"/>
                  <line x1="29" y1="29" x2="33" y2="33"/>
                  <line x1="33" y1="7" x2="29" y2="11"/>
                  <line x1="11" y1="29" x2="7" y2="33"/>
                </g>
              </svg>
              <div class="kicker">THE OLDEST COUNTERFEIT</div>
              <h3 class="card-title">Sun Worship</h3>
              <div class="body-box">From Babel to Easter morning to the obelisks of every Western capital — the religion that never died, only changed costumes. Trace the same rite from Heliopolis to your Sunday service.</div>
            </div>
            <div class="card-cta">OPEN THE SECTION →</div>
          </a>

          <!-- Trinity -->
          <a class="home-card card-trinity" href="/trinity">
            <div>
              <svg class="hex" viewBox="0 0 64 64">
                <circle cx="32" cy="32" r="30" fill="none" stroke="#7ab894" stroke-width="1.5"/>
                <path d="M 32 10 L 51 42 L 13 42 Z" fill="none" stroke="#7ab894" stroke-width="1.5"/>
                <path d="M 32 54 L 13 22 L 51 22 Z" fill="none" stroke="#7ab894" stroke-width="1.5"/>
                <circle cx="32" cy="32" r="4" fill="#7ab894"/>
              </svg>
              <div class="kicker">EXAMINING A SACRED COW</div>
              <h3 class="card-title">The Trinity</h3>
              <p class="card-body">The symbol above is older than the church that claims it. So is the doctrine. <em>Neither was given by Scripture.</em> Trace where both actually came from — and what the text says instead.</p>
            </div>
            <div class="card-cta">OPEN THE SECTION →</div>
          </a>

          <!-- Catholicism -->
          <a class="home-card card-catholic" href="/catholicism">
            <div>
              <svg class="dome" viewBox="0 0 70 70">
                <line x1="35" y1="4" x2="35" y2="18" stroke="#c9a961" stroke-width="1.5"/>
                <line x1="31" y1="9" x2="39" y2="9" stroke="#c9a961" stroke-width="1.5"/>
                <path d="M 18 40 Q 18 18, 35 18 Q 52 18, 52 40 Z" fill="none" stroke="#c9a961" stroke-width="1.5"/>
                <path d="M 18 40 Q 18 22, 35 22 Q 52 22, 52 40" fill="none" stroke="#c9a961" stroke-width="0.5" opacity="0.5"/>
                <rect x="12" y="40" width="46" height="4" fill="none" stroke="#c9a961" stroke-width="1.5"/>
                <line x1="18" y1="44" x2="18" y2="62" stroke="#c9a961" stroke-width="1"/>
                <line x1="26" y1="44" x2="26" y2="62" stroke="#c9a961" stroke-width="1"/>
                <line x1="35" y1="44" x2="35" y2="62" stroke="#c9a961" stroke-width="1"/>
                <line x1="44" y1="44" x2="44" y2="62" stroke="#c9a961" stroke-width="1"/>
                <line x1="52" y1="44" x2="52" y2="62" stroke="#c9a961" stroke-width="1"/>
                <rect x="8" y="62" width="54" height="4" fill="none" stroke="#c9a961" stroke-width="1.5"/>
              </svg>
              <div class="kicker">THE MOTHER CHURCH</div>
              <h3 class="card-title">Rome Never Left</h3>
              <p class="card-body">Protestants broke from the Pope but kept his Sunday, his Christmas, his Easter, his Trinity, his canon. The Reformation rearranged the furniture in a house Rome built.</p>
            </div>
            <div class="card-cta">OPEN THE SECTION →</div>
          </a>

          <!-- American Idolatry -->
          <a class="home-card card-american" href="/american-idolatry">
            <div class="stripes-top"></div>
            <div class="nav-blue">
              <div class="stars">★ ★ ★ ★ ★ ★ ★ ★ ★</div>
              <div class="section-label">AMERICAN IDOLATRY</div>
            </div>
            <div class="red-rule"></div>
            <div class="inner">
              <div>
                <h3 class="card-title">The Babylon We Were Born Into</h3>
                <div class="count">NINE STUDIES · ONE CORNERSTONE</div>
                <p class="body-txt">The patriotic holidays, monuments, oaths, and symbols Christians have been told are sacred — examined against what Scripture actually permits.</p>
              </div>
              <div class="card-cta">OPEN THE SECTION →</div>
            </div>
            <div class="red-rule"></div>
            <div class="stripes-bottom"></div>
          </a>

          <!-- Holidays -->
          <a class="home-card card-holidays" href="/holidays">
            <div>
              <svg class="moon-trees" viewBox="0 0 150 110">
                <circle cx="75" cy="42" r="22" fill="#f0e6b4" opacity="0.85"/>
                <circle cx="72" cy="38" r="3" fill="#d9cc96" opacity="0.5"/>
                <circle cx="82" cy="48" r="2" fill="#d9cc96" opacity="0.5"/>
                <g fill="#030d0a">
                  <path d="M 10 110 L 10 80 L 5 80 L 15 65 L 10 65 L 20 50 L 15 50 L 25 35 L 35 50 L 30 50 L 40 65 L 35 65 L 45 80 L 40 80 L 40 110 Z"/>
                  <path d="M 45 110 L 45 85 L 40 85 L 50 70 L 45 70 L 55 55 L 50 55 L 58 42 L 66 55 L 61 55 L 71 70 L 66 70 L 76 85 L 71 85 L 71 110 Z" opacity="0.92"/>
                  <path d="M 115 110 L 115 75 L 110 75 L 120 60 L 115 60 L 123 45 L 130 60 L 125 60 L 135 75 L 130 75 L 140 90 L 135 90 L 135 110 Z"/>
                </g>
              </svg>
              <div class="kicker">THE FEASTS MAN MADE</div>
              <h3 class="card-title">Christmas, Easter, and the Calendar <em>Yahuah Never Gave</em></h3>
              <p class="card-body">Evergreens cut for a winter festival. Sunrise services at the spring equinox. Every holiday modern Christianity celebrates — weighed against the <em>moedim</em> Scripture actually commands.</p>
            </div>
            <div class="card-cta">OPEN THE SECTION →</div>
          </a>
        </div>
      </section>

      <div class="divider-line"></div>

      <!-- ========= SECTION 3: THE RESTORATION ========= -->
      <section class="home-section">
        <div class="section-header">
          <div class="section-eyebrow">What Scripture Actually Teaches</div>
          <div class="section-ornament"><span class="line"></span><span class="dot"></span><span class="line"></span></div>
          <h2 class="section-title">The Restoration</h2>
          <p class="section-sub">Recovering the language, the assembly, and the method — the tools the church kept from you.</p>
        </div>

        <div class="home-grid">
          <!-- Hebrew -->
          <a class="home-card card-hebrew" href="/hebrew">
            <div>
              <div class="paleo">𐤉𐤄𐤅𐤄</div>
              <div class="kicker">THE ORIGINAL TONGUE</div>
              <h3 class="card-title">Hebrew</h3>
              <p class="card-body">The pictographs above are <em>YHWH</em> in paleo-Hebrew — one example of what translation erased. But the language goes deeper: root systems, idioms, word pictures, and a Hebraic worldview Greek and English cannot carry across. Because reading Scripture in English is reading <em>a translation of a translation.</em></p>
            </div>
            <div class="card-cta">OPEN THE SECTION →</div>
          </a>

          <!-- Pathways -->
          <a class="home-card card-pathways" href="/pathways">
            <div>
              <svg class="magnifier" viewBox="0 0 80 80">
                <defs>
                  <radialGradient id="lensglow" cx="50%" cy="50%" r="50%">
                    <stop offset="0%" stop-color="#f5e6b0" stop-opacity="0.3"/>
                    <stop offset="100%" stop-color="#f5e6b0" stop-opacity="0"/>
                  </radialGradient>
                </defs>
                <rect x="20" y="34" width="24" height="14" fill="#e8c579" opacity="0.2"/>
                <line x1="23" y1="38" x2="41" y2="38" stroke="#c9a961" stroke-width="0.7" opacity="0.8"/>
                <line x1="23" y1="41" x2="41" y2="41" stroke="#c9a961" stroke-width="0.7" opacity="0.8"/>
                <line x1="23" y1="44" x2="38" y2="44" stroke="#c9a961" stroke-width="0.7" opacity="0.8"/>
                <circle cx="32" cy="34" r="22" fill="url(#lensglow)"/>
                <circle cx="32" cy="34" r="20" fill="none" stroke="#e8c579" stroke-width="2.5"/>
                <circle cx="32" cy="34" r="17" fill="none" stroke="#c9a961" stroke-width="0.5" opacity="0.6"/>
                <line x1="48" y1="50" x2="68" y2="70" stroke="#e8c579" stroke-width="4" stroke-linecap="round"/>
                <line x1="48" y1="50" x2="68" y2="70" stroke="#8a7540" stroke-width="2" stroke-linecap="round"/>
              </svg>
              <div class="kicker">LEARN TO DIG</div>
              <h3 class="card-title">How to Pull the Truth<br>Out <em>Yourself</em></h3>
              <div class="hebrew-stones">
                <span>חֶסֶד</span><span>·</span><span>לֵב</span><span>·</span><span>שָׁלוֹם</span>
              </div>
              <p class="card-body">A step-by-step series of guided word studies — Strong's, lexicons, original Hebrew and Greek, cross-referencing, and context — each one teaching you a skill you keep. <em>Not conclusions handed down. The tools placed in your hand until you don't need a teacher anymore.</em></p>
            </div>
            <div class="card-cta">BEGIN THE PATHWAY →</div>
          </a>

          <!-- Ekklesia -->
          <a class="home-card card-ekklesia" href="/ekklesia">
            <div>
              <svg class="doorway" viewBox="0 0 60 80">
                <path d="M 8 76 L 8 26 Q 8 8, 30 8 Q 52 8, 52 26 L 52 76" fill="none" stroke="#5a3a1e" stroke-width="1.5"/>
                <path d="M 14 76 L 14 30 Q 14 14, 30 14 Q 46 14, 46 30 L 46 76" fill="none" stroke="#5a3a1e" stroke-width="1" opacity="0.5"/>
                <path d="M 26 8 L 34 8 L 32 14 L 28 14 Z" fill="#5a3a1e" opacity="0.7"/>
                <rect x="16" y="30" width="28" height="46" fill="#c9a961" opacity="0.15"/>
                <line x1="4" y1="76" x2="56" y2="76" stroke="#5a3a1e" stroke-width="1.5"/>
                <circle cx="22" cy="56" r="2" fill="#5a3a1e" opacity="0.6"/>
                <rect x="20" y="58" width="4" height="8" fill="#5a3a1e" opacity="0.6"/>
                <circle cx="30" cy="54" r="2" fill="#5a3a1e" opacity="0.6"/>
                <rect x="28" y="56" width="4" height="10" fill="#5a3a1e" opacity="0.6"/>
                <circle cx="38" cy="56" r="2" fill="#5a3a1e" opacity="0.6"/>
                <rect x="36" y="58" width="4" height="8" fill="#5a3a1e" opacity="0.6"/>
              </svg>
              <div class="kicker">A BUSINESS WITH A STEEPLE</div>
              <h3 class="card-title">The Church Isn't a Body.<br>It's a Corporation.</h3>
              <p class="card-body">Drive-thru prayer lines. Multi-million-dollar building campaigns. 501(c)(3) filings traded for pulpit silence. Pastor salaries indexed to tithe growth. Somewhere between the upper room and the mega-campus, the <em style="font-style:italic;">ekklesia</em> Yahushua founded became a business with tax benefits.</p>
            </div>
            <div class="card-cta">OPEN THE SECTION →</div>
          </a>
        </div>
      </section>

      <div class="divider-line"></div>

      <!-- ========= SECTION 4: LIVING IT OUT ========= -->
      <section class="home-section">
        <div class="section-header">
          <div class="section-eyebrow">The Walk, Not Just the Words</div>
          <div class="section-ornament"><span class="line"></span><span class="dot"></span><span class="line"></span></div>
          <h2 class="section-title">Living It Out</h2>
          <p class="section-sub">Application, discernment, and the verses that change everything once you see them.</p>
        </div>

        <div class="home-grid">
          <!-- Foreign Fire -->
          <a class="home-card card-fire" href="/foreign-fire">
            <div>
              <div class="kicker">STRANGE FIRE</div>
              <h3 class="card-title">Foreign <em>Fire</em></h3>
              <p class="card-body">Zoroastrian dualism shaping Christian eschatology. Kabbalistic mysticism laundered through centuries into modern worship. A Persian holiday bound inside the canon. Religious systems that crossed the border while the watchmen slept — every one of them carrying fire Yahuah never commanded.</p>
              <div class="verse">LEVITICUS 10:1–2</div>
            </div>
            <div class="card-cta">OPEN THE SECTION →</div>
          </a>

          <!-- Hidden in Plain Sight (Pixar, Fantasy, Sci-Fi, Symbols) -->
          <a class="home-card card-children" href="/hidden-in-plain-sight/">
            <div>
              <svg class="castle" viewBox="0 0 90 90">
                <circle cx="68" cy="18" r="10" fill="#c8a0f5" opacity="0.4"/>
                <g fill="#0a1428" stroke="#a88ad6" stroke-width="0.5">
                  <rect x="20" y="40" width="50" height="40"/>
                  <rect x="12" y="30" width="14" height="50"/>
                  <rect x="64" y="26" width="14" height="54"/>
                  <rect x="38" y="22" width="14" height="58"/>
                  <path d="M 10 30 L 19 18 L 28 30 Z"/>
                  <path d="M 36 22 L 45 8 L 54 22 Z"/>
                  <path d="M 62 26 L 71 14 L 80 26 Z"/>
                  <rect x="14" y="40" width="2" height="4"/>
                  <rect x="18" y="40" width="2" height="4"/>
                  <rect x="22" y="40" width="2" height="4"/>
                </g>
                <rect x="42" y="40" width="2.5" height="4" fill="#ffc8c8" opacity="0.85"/>
                <rect x="46" y="40" width="2.5" height="4" fill="#ffc8c8" opacity="0.85"/>
                <rect x="42" y="52" width="2.5" height="4" fill="#ffc8c8" opacity="0.6"/>
                <rect x="68" y="48" width="2.5" height="4" fill="#ffc8c8" opacity="0.7"/>
                <rect x="16" y="52" width="2.5" height="4" fill="#ffc8c8" opacity="0.7"/>
                <path d="M 30 80 Q 35 75, 40 80 Q 45 85, 50 80" fill="none" stroke="#a88ad6" stroke-width="0.8" opacity="0.5"/>
              </svg>
              <div class="kicker">RAISED ON MAGIC</div>
              <h3 class="card-title">What They Taught Your Kids <em>While You Weren't Looking</em></h3>
              <p class="card-body">Wizards as heroes. Witches as misunderstood. Gods as cartoons. The stories shaping the next generation aren't neutral — they're catechism.</p>
            </div>
            <div class="card-cta">OPEN THE SECTION →</div>
          </a>

          <!-- Hidden Gems -->
          <a class="home-card card-gems" href="/hidden-gems">
            <div>
              <svg class="gem" viewBox="0 0 70 80">
                <defs>
                  <linearGradient id="gemshine" x1="0%" y1="0%" x2="100%" y2="100%">
                    <stop offset="0%" stop-color="#ffffff" stop-opacity="0.9"/>
                    <stop offset="50%" stop-color="#b8d4f5" stop-opacity="0.7"/>
                    <stop offset="100%" stop-color="#4a6b9c" stop-opacity="0.9"/>
                  </linearGradient>
                </defs>
                <path d="M 15 30 L 35 10 L 55 30 L 35 72 Z" fill="url(#gemshine)" stroke="#f0f5ff" stroke-width="1"/>
                <line x1="15" y1="30" x2="55" y2="30" stroke="#f0f5ff" stroke-width="1" opacity="0.7"/>
                <line x1="35" y1="10" x2="35" y2="72" stroke="#f0f5ff" stroke-width="0.6" opacity="0.5"/>
                <line x1="25" y1="20" x2="25" y2="30" stroke="#f0f5ff" stroke-width="0.5" opacity="0.6"/>
                <line x1="45" y1="20" x2="45" y2="30" stroke="#f0f5ff" stroke-width="0.5" opacity="0.6"/>
                <line x1="15" y1="30" x2="35" y2="72" stroke="#f0f5ff" stroke-width="0.6" opacity="0.4"/>
                <line x1="55" y1="30" x2="35" y2="72" stroke="#f0f5ff" stroke-width="0.6" opacity="0.4"/>
                <path d="M 22 24 L 30 14 L 34 18 L 28 28 Z" fill="#ffffff" opacity="0.55"/>
                <circle cx="58" cy="14" r="1" fill="#ffffff"/>
                <circle cx="12" cy="18" r="0.8" fill="#ffffff" opacity="0.7"/>
                <circle cx="62" cy="40" r="0.8" fill="#ffffff" opacity="0.6"/>
              </svg>
              <div class="kicker">FOUND BY THOSE WHO DIG</div>
              <h3 class="card-title">Hidden Gems</h3>
              <p class="card-body">The studies you won't find anywhere else — because nobody else dug for them. Passages the commentaries skipped. Hebrew roots the translators smoothed over. Parallels no pulpit will draw. What only surfaces when you stop trusting the surface and start mining.</p>
            </div>
            <div class="card-cta">BROWSE THE GEMS →</div>
          </a>
        </div>
      </section>

    </div>
  </div>

  <style>
    /* ====================================================
       HOMEPAGE FOUR-SECTION BAND
       ==================================================== */
    .home-sections {
      --bg: #0a0806;
      --parchment: #f2e9d4;
      --gold: #c9a961;
      --gold-bright: #e8c579;
      --gold-dim: #8a7540;
      --ink: #1a1410;
      --red: #8b1a1a;

      background: var(--bg);
      color: var(--parchment);
      font-family: 'Cormorant Garamond', Georgia, serif;
      line-height: 1.5;
    }
    .home-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 40px;
    }
    .home-section {
      padding: 90px 0 60px;
    }
    .section-header {
      text-align: center;
      margin-bottom: 60px;
    }
    .section-eyebrow {
      font-family: 'Cinzel', serif;
      font-size: 11px;
      letter-spacing: 6px;
      color: var(--gold);
      margin-bottom: 14px;
      font-weight: 600;
      text-transform: uppercase;
    }
    .section-title {
      font-family: 'Playfair Display', serif;
      font-size: 44px;
      font-style: italic;
      font-weight: 400;
      color: var(--parchment);
      margin: 0 0 14px;
    }
    .section-sub {
      font-size: 17px;
      color: rgba(242, 233, 212, 0.6);
      max-width: 560px;
      margin: 0 auto;
      font-style: italic;
    }
    .section-ornament {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      margin-bottom: 14px;
    }
    .section-ornament .line {
      width: 50px;
      height: 1px;
      background: var(--gold-dim);
    }
    .section-ornament .dot {
      width: 4px;
      height: 4px;
      background: var(--gold);
      transform: rotate(45deg);
    }

    .home-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
    }

    .home-card {
      aspect-ratio: 3 / 4;
      border-radius: 2px;
      position: relative;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 38px 34px;
      text-align: center;
      text-decoration: none;
      color: inherit;
      transition: transform 0.4s ease, box-shadow 0.4s ease;
    }
    .home-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
    }

    .home-card .kicker {
      font-family: 'Cinzel', serif;
      font-size: 10px;
      letter-spacing: 5px;
      font-weight: 600;
      text-transform: uppercase;
      margin-bottom: 16px;
    }
    .home-card .card-title {
      font-family: 'Playfair Display', serif;
      font-size: 34px;
      line-height: 1.08;
      font-weight: 700;
      margin-bottom: 22px;
    }
    .home-card .card-title em {
      font-style: italic;
      font-weight: 400;
    }
    .home-card .card-body {
      font-size: 15.5px;
      line-height: 1.55;
      font-family: 'Cormorant Garamond', serif;
      margin-bottom: 18px;
    }
    .home-card .card-cta {
      font-family: 'Cinzel', serif;
      font-size: 10px;
      letter-spacing: 4px;
      font-weight: 600;
      padding-top: 18px;
      border-top: 1px solid currentColor;
      opacity: 0.85;
    }

    .divider-line {
      height: 1px;
      background: linear-gradient(90deg, transparent 0%, rgba(201, 169, 97, 0.3) 50%, transparent 100%);
      margin: 40px 0;
    }

    /* ---------- Watchman's Desk ---------- */
    .card-watchman {
      background: var(--parchment);
      color: var(--ink);
    }
    .card-watchman .watchman-mast {
      font-family: 'Cinzel', serif;
      font-size: 10px;
      letter-spacing: 4px;
      color: var(--red);
      padding: 10px 0;
      border-top: 1px solid var(--ink);
      border-bottom: 1px solid var(--ink);
      margin-bottom: 18px;
    }
    .card-watchman .date {
      font-family: 'Cinzel', serif;
      font-size: 10px;
      letter-spacing: 3px;
      margin-bottom: 18px;
    }
    .card-watchman .card-title {
      font-size: 28px;
      font-style: italic;
      margin-bottom: 16px;
    }
    .card-watchman .article-body {
      text-align: left;
      font-family: 'EB Garamond', serif;
      font-size: 14px;
      line-height: 1.5;
      color: #2a2016;
      border-top: 1px solid var(--ink);
      padding-top: 16px;
    }
    .card-watchman .article-body::first-letter {
      font-family: 'Playfair Display', serif;
      font-size: 40px;
      float: left;
      line-height: 0.9;
      padding: 4px 6px 0 0;
      font-weight: 900;
    }
    .card-watchman .card-cta {
      color: var(--red);
      border-top-color: var(--ink);
    }

    /* ---------- Studies ---------- */
    .card-studies {
      background: linear-gradient(180deg, #f5ead0 0%, #e8d9b2 100%);
      color: var(--ink);
    }
    .card-studies .aleph {
      font-size: 40px;
      color: var(--gold-dim);
      margin-bottom: 14px;
      font-family: serif;
    }
    .card-studies .card-title {
      font-style: italic;
      font-weight: 400;
      font-size: 32px;
    }
    .card-studies .card-body { color: #2a2016; }
    .card-studies .latest-badge {
      background: rgba(255, 255, 255, 0.6);
      padding: 14px 12px;
      border-left: 3px solid var(--red);
      text-align: left;
      margin: 10px 0;
    }
    .card-studies .latest-badge .label {
      font-family: 'Cinzel', serif;
      font-size: 9px;
      letter-spacing: 4px;
      color: var(--ink);
    }
    .card-studies .latest-badge .title {
      font-family: 'Playfair Display', serif;
      font-style: italic;
      font-size: 16px;
      color: var(--ink);
      margin-top: 4px;
    }
    .card-studies .card-cta {
      color: var(--red);
      border-top-color: rgba(26, 20, 16, 0.2);
    }

    /* ---------- Library ---------- */
    .card-library {
      background: linear-gradient(180deg, #4a2e18 0%, #2a1a0a 100%);
      color: #e8c579;
      border: 1px solid rgba(232, 197, 121, 0.2);
    }
    .card-library .diamonds {
      color: var(--gold);
      font-size: 14px;
      letter-spacing: 6px;
      margin-bottom: 14px;
    }
    .card-library .kicker { color: var(--gold-dim); }
    .card-library .card-title {
      font-style: italic;
      font-weight: 400;
    }
    .card-library .sub-italic {
      font-style: italic;
      color: rgba(232, 197, 121, 0.8);
      font-size: 15px;
      margin-bottom: 14px;
    }
    .card-library .card-body {
      color: rgba(232, 197, 121, 0.75);
      font-style: italic;
    }
    .card-library .card-cta {
      color: var(--gold);
      border-top-color: rgba(232, 197, 121, 0.3);
    }

    /* ---------- Christian or Demonic ---------- */
    .card-demonic {
      background: radial-gradient(ellipse at 50% 40%, #6b1818 0%, #2a0808 80%, #0f0303 100%);
      color: var(--parchment);
      justify-content: flex-start;
    }
    .card-demonic .card-title {
      color: #ffffff;
      font-size: 38px;
    }
    .card-demonic .card-title em {
      color: #ff6a3d;
      font-style: italic;
    }
    .card-demonic .body-dark {
      color: rgba(242, 233, 212, 0.85);
      font-size: 16px;
      margin-bottom: 40px;
    }
    .card-demonic .body-dark em {
      color: #ff8a5c;
      font-style: italic;
    }
    .card-demonic .pillars {
      font-size: 14px;
      letter-spacing: 1px;
      margin-top: auto;
      color: rgba(242, 233, 212, 0.7);
      padding-bottom: 20px;
      border-bottom: 1px solid rgba(242, 233, 212, 0.2);
    }
    .card-demonic .card-cta {
      color: #ff6a3d;
      border-top-color: transparent;
    }

    /* ---------- Sun Worship ---------- */
    .card-sun {
      background: radial-gradient(ellipse at 50% 25%, #ffb347 0%, #d97a1f 30%, #6b2f0e 80%, #1a0a04 100%);
      color: var(--parchment);
      position: relative;
    }
    .card-sun::before {
      content: "";
      position: absolute;
      top: 20px;
      left: 50%;
      transform: translateX(-50%);
      width: 180px;
      height: 180px;
      background: radial-gradient(circle, rgba(255, 220, 150, 0.6) 0%, transparent 60%);
      pointer-events: none;
    }
    .card-sun .sun-icon {
      width: 40px;
      height: 40px;
      margin: 0 auto 10px;
      position: relative;
      z-index: 2;
    }
    .card-sun .card-title {
      color: #fff;
      font-size: 36px;
      font-weight: 700;
      position: relative;
      z-index: 2;
    }
    .card-sun .body-box {
      background: rgba(255, 220, 150, 0.15);
      padding: 16px;
      border: 1px solid rgba(255, 220, 150, 0.2);
      font-size: 14.5px;
      line-height: 1.5;
      position: relative;
      z-index: 2;
    }
    .card-sun .card-cta {
      color: #ffdca0;
      position: relative;
      z-index: 2;
    }

    /* ---------- Trinity ---------- */
    .card-trinity {
      background: radial-gradient(ellipse at 50% 30%, #0d3d2b 0%, #04180f 80%);
      color: #a8d4b8;
    }
    .card-trinity .hex {
      width: 64px;
      height: 64px;
      margin: 0 auto 20px;
    }
    .card-trinity .kicker { color: #7ab894; }
    .card-trinity .card-title {
      color: #e8f5ec;
      font-size: 40px;
    }
    .card-trinity .card-body { color: rgba(200, 230, 210, 0.75); }
    .card-trinity .card-body em {
      font-style: italic;
      font-weight: 600;
      color: #b8e0c8;
    }
    .card-trinity .card-cta {
      color: #7ab894;
      border-top-color: rgba(122, 184, 148, 0.3);
    }

    /* ---------- Catholicism ---------- */
    .card-catholic {
      background:
        radial-gradient(ellipse at 50% 30%, rgba(139, 26, 26, 0.6) 0%, transparent 50%),
        linear-gradient(180deg, #3a0808 0%, #1a0404 70%, #0a0202 100%);
      color: #e8c579;
      position: relative;
      overflow: hidden;
    }
    .card-catholic::before {
      content: "";
      position: absolute;
      top: 40px;
      left: 50%;
      transform: translateX(-50%);
      width: 120px;
      height: 120px;
      background: radial-gradient(circle, rgba(201, 169, 97, 0.15) 0%, transparent 70%);
      pointer-events: none;
    }
    .card-catholic .dome {
      width: 70px;
      height: 70px;
      margin: 0 auto 16px;
      position: relative;
      z-index: 2;
    }
    .card-catholic .kicker {
      color: #c9a961;
      position: relative;
      z-index: 2;
    }
    .card-catholic .card-title {
      color: #f5e6c0;
      font-style: italic;
      font-weight: 400;
      font-size: 40px;
      position: relative;
      z-index: 2;
    }
    .card-catholic .card-body {
      color: rgba(232, 197, 121, 0.8);
      position: relative;
      z-index: 2;
    }
    .card-catholic .card-cta {
      color: #c9a961;
      border-top-color: rgba(201, 169, 97, 0.3);
      position: relative;
      z-index: 2;
    }

    /* ---------- American Idolatry ---------- */
    .card-american {
      background: var(--parchment);
      color: var(--ink);
      padding: 0;
      display: flex;
      flex-direction: column;
    }
    .card-american .stripes-top {
      background: repeating-linear-gradient(90deg, #c8102e 0 14%, #ffffff 14% 28%);
      height: 18px;
    }
    .card-american .stripes-bottom {
      background: repeating-linear-gradient(90deg, #1e3a6f 0 14%, #ffffff 14% 28%);
      height: 18px;
    }
    .card-american .nav-blue {
      background: #1e3a6f;
      color: #fff;
      padding: 14px 0;
      text-align: center;
    }
    .card-american .stars {
      color: #fff;
      font-size: 12px;
      letter-spacing: 6px;
      margin-bottom: 6px;
    }
    .card-american .section-label {
      font-family: 'Cinzel', serif;
      font-size: 10px;
      letter-spacing: 4px;
      font-weight: 600;
    }
    .card-american .red-rule {
      background: #c8102e;
      height: 4px;
    }
    .card-american .inner {
      padding: 30px 26px;
      text-align: center;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .card-american .card-title {
      color: #1e3a6f;
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 10px;
    }
    .card-american .count {
      color: #c8102e;
      font-family: 'Cinzel', serif;
      font-size: 10px;
      letter-spacing: 4px;
      margin-bottom: 14px;
    }
    .card-american .body-txt {
      font-size: 14px;
      line-height: 1.5;
      color: #2a2016;
    }
    .card-american .card-cta {
      color: #c8102e;
      border-top: none;
    }

    /* ---------- Holidays ---------- */
    .card-holidays {
      background:
        radial-gradient(ellipse at 50% 28%, rgba(240, 230, 180, 0.15) 0%, transparent 45%),
        linear-gradient(180deg, #0e2418 0%, #071a14 60%, #030d0a 100%);
      color: #c8dfc8;
      position: relative;
      overflow: hidden;
    }
    .card-holidays::before {
      content: "";
      position: absolute;
      top: 14%;
      left: 50%;
      transform: translateX(-50%);
      width: 110px;
      height: 110px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(240, 230, 180, 0.35) 0%, rgba(240, 230, 180, 0.05) 60%, transparent 75%);
      pointer-events: none;
    }
    .card-holidays .moon-trees {
      width: 150px;
      height: 110px;
      margin: 0 auto 6px;
      position: relative;
      z-index: 2;
    }
    .card-holidays .kicker {
      color: #8fae8a;
      position: relative;
      z-index: 2;
    }
    .card-holidays .card-title {
      color: #ecf5e8;
      font-weight: 700;
      font-size: 26px;
      line-height: 1.18;
      position: relative;
      z-index: 2;
    }
    .card-holidays .card-title em {
      font-style: italic;
      color: #b8d4b0;
      font-weight: 400;
    }
    .card-holidays .card-body {
      color: rgba(200, 223, 200, 0.82);
      font-size: 14.5px;
      position: relative;
      z-index: 2;
    }
    .card-holidays .card-body em {
      font-style: italic;
      color: #c8dfc8;
    }
    .card-holidays .card-cta {
      color: #8fae8a;
      border-top-color: rgba(143, 174, 138, 0.3);
    }

    /* ---------- Hebrew ---------- */
    .card-hebrew {
      background: linear-gradient(180deg, #f5e9c6 0%, #e8d5a0 100%);
      color: var(--ink);
    }
    .card-hebrew .paleo {
      font-size: 56px;
      color: #5a3a1a;
      letter-spacing: 10px;
      margin-bottom: 10px;
      font-family: serif;
      font-weight: 300;
    }
    .card-hebrew .kicker { color: #5a3a1a; }
    .card-hebrew .card-title {
      font-style: italic;
      font-weight: 400;
    }
    .card-hebrew .card-body {
      color: #2a2016;
      font-size: 14.5px;
      text-align: left;
    }
    .card-hebrew .card-body em {
      font-style: italic;
      font-weight: 600;
    }
    .card-hebrew .card-cta {
      color: #5a3a1a;
      border-top-color: rgba(90, 58, 26, 0.3);
    }

    /* ---------- Pathways ---------- */
    .card-pathways {
      background:
        radial-gradient(ellipse at 50% 40%, rgba(232, 197, 121, 0.25) 0%, transparent 55%),
        linear-gradient(180deg, #2a1f10 0%, #1a130a 100%);
      color: #f0dba8;
      position: relative;
      overflow: hidden;
    }
    .card-pathways::before {
      content: "";
      position: absolute;
      inset: 0;
      background:
        radial-gradient(circle at 20% 70%, rgba(232, 197, 121, 0.1) 0%, transparent 30%),
        radial-gradient(circle at 80% 75%, rgba(232, 197, 121, 0.08) 0%, transparent 25%);
      pointer-events: none;
    }
    .card-pathways .magnifier {
      width: 80px;
      height: 80px;
      margin: 0 auto 14px;
      position: relative;
      z-index: 2;
    }
    .card-pathways .kicker {
      color: #c9a961;
      position: relative;
      z-index: 2;
    }
    .card-pathways .card-title {
      color: #f5e6c0;
      font-weight: 700;
      font-size: 30px;
      line-height: 1.15;
      position: relative;
      z-index: 2;
    }
    .card-pathways .card-title em {
      color: #e8c579;
      font-style: italic;
      font-weight: 400;
    }
    .card-pathways .card-body {
      color: rgba(240, 219, 168, 0.85);
      font-size: 15px;
      position: relative;
      z-index: 2;
    }
    .card-pathways .card-body em {
      font-style: italic;
      color: #e8c579;
    }
    .card-pathways .hebrew-stones {
      display: flex;
      justify-content: center;
      gap: 14px;
      margin: 14px 0;
      position: relative;
      z-index: 2;
    }
    .card-pathways .hebrew-stones span {
      color: #c9a961;
      font-size: 14px;
      font-family: serif;
      opacity: 0.75;
    }
    .card-pathways .card-cta {
      color: #e8c579;
      border-top-color: rgba(232, 197, 121, 0.3);
      position: relative;
      z-index: 2;
    }

    /* ---------- Ekklesia ---------- */
    .card-ekklesia {
      background:
        radial-gradient(ellipse at 50% 90%, rgba(201, 169, 97, 0.15) 0%, transparent 60%),
        linear-gradient(180deg, #f2e5c4 0%, #ddc89a 100%);
      color: #3a2a18;
      position: relative;
      overflow: hidden;
    }
    .card-ekklesia::before {
      content: "ἐκκλησία";
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-family: 'EB Garamond', serif;
      font-size: 80px;
      color: rgba(90, 58, 26, 0.07);
      letter-spacing: 2px;
      pointer-events: none;
      font-style: italic;
    }
    .card-ekklesia .doorway {
      width: 60px;
      height: 80px;
      margin: 0 auto 14px;
      position: relative;
      z-index: 2;
    }
    .card-ekklesia .kicker {
      color: #7a4a1e;
      position: relative;
      z-index: 2;
    }
    .card-ekklesia .card-title {
      color: #2a1810;
      font-style: italic;
      font-weight: 400;
      font-size: 30px;
      line-height: 1.1;
      position: relative;
      z-index: 2;
    }
    .card-ekklesia .card-body {
      color: #3a2a18;
      position: relative;
      z-index: 2;
    }
    .card-ekklesia .card-cta {
      color: #7a4a1e;
      border-top-color: rgba(122, 74, 30, 0.3);
      position: relative;
      z-index: 2;
    }

    /* ---------- Foreign Fire ---------- */
    .card-fire {
      background: radial-gradient(ellipse at 50% 60%, #1e3a6f 0%, #0a1428 70%, #020610 100%);
      color: #c8d9f5;
    }
    .card-fire .kicker {
      color: #6a8fcb;
      letter-spacing: 5px;
    }
    .card-fire .card-title {
      color: #fff;
      font-size: 38px;
      font-weight: 700;
    }
    .card-fire .card-title em {
      color: #9ac2ff;
      font-style: italic;
      font-weight: 400;
    }
    .card-fire .card-body { color: rgba(200, 217, 245, 0.85); }
    .card-fire .verse {
      color: #6a8fcb;
      font-family: 'Cinzel', serif;
      font-size: 11px;
      letter-spacing: 5px;
      margin-top: 10px;
    }
    .card-fire .card-cta {
      color: #6a8fcb;
      border-top-color: rgba(106, 143, 203, 0.3);
    }

    /* ---------- Children / Disney ---------- */
    .card-children {
      background:
        radial-gradient(ellipse at 50% 80%, rgba(138, 90, 180, 0.4) 0%, transparent 60%),
        linear-gradient(180deg, #1a0a2e 0%, #0a1a2e 100%);
      color: #d9c0ff;
      position: relative;
      overflow: hidden;
    }
    .card-children::before {
      content: "";
      position: absolute;
      top: 10%;
      left: 50%;
      transform: translateX(-50%);
      width: 140px;
      height: 140px;
      background: radial-gradient(circle, rgba(200, 150, 255, 0.2) 0%, transparent 60%);
      pointer-events: none;
    }
    .card-children .castle {
      width: 90px;
      height: 90px;
      margin: 0 auto 14px;
      position: relative;
      z-index: 2;
    }
    .card-children .kicker {
      color: #a88ad6;
      position: relative;
      z-index: 2;
    }
    .card-children .card-title {
      color: #f0e0ff;
      font-weight: 700;
      font-size: 28px;
      line-height: 1.15;
      position: relative;
      z-index: 2;
    }
    .card-children .card-title em {
      font-style: italic;
      color: #c8a0f5;
      font-weight: 400;
    }
    .card-children .card-body {
      color: rgba(217, 192, 255, 0.8);
      font-size: 14.5px;
      position: relative;
      z-index: 2;
    }
    .card-children .card-cta {
      color: #a88ad6;
      border-top-color: rgba(168, 138, 214, 0.3);
      position: relative;
      z-index: 2;
    }

    /* ---------- Hidden Gems ---------- */
    .card-gems {
      background:
        radial-gradient(ellipse at 50% 35%, rgba(130, 180, 220, 0.2) 0%, transparent 55%),
        linear-gradient(180deg, #0f1a38 0%, #050a1a 100%);
      color: #c8d9f5;
      position: relative;
      overflow: hidden;
    }
    .card-gems::before {
      content: "";
      position: absolute;
      top: 18%;
      left: 50%;
      transform: translateX(-50%);
      width: 140px;
      height: 140px;
      background: radial-gradient(circle, rgba(180, 220, 255, 0.3) 0%, transparent 65%);
      pointer-events: none;
    }
    .card-gems .gem {
      width: 70px;
      height: 80px;
      margin: 0 auto 14px;
      position: relative;
      z-index: 2;
    }
    .card-gems .kicker {
      color: #7a9ed0;
      position: relative;
      z-index: 2;
    }
    .card-gems .card-title {
      color: #f0f5ff;
      font-style: italic;
      font-weight: 400;
      font-size: 36px;
      position: relative;
      z-index: 2;
    }
    .card-gems .card-body {
      color: rgba(200, 217, 245, 0.82);
      position: relative;
      z-index: 2;
    }
    .card-gems .card-cta {
      color: #7a9ed0;
      border-top-color: rgba(122, 158, 208, 0.3);
      position: relative;
      z-index: 2;
    }

    /* Responsive */
    @media (max-width: 1100px) {
      .home-grid { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 700px) {
      .home-container { padding: 0 20px; }
      .home-grid { grid-template-columns: 1fr; }
      .home-section { padding: 60px 0 40px; }
      .section-title { font-size: 32px; }
    }
  </style>


</BaseLayout>
